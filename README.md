# obsidian-claude-vault-assistant
An Obsidian plugin that lets users define reusable prompt files and run them against their vault or the currently active note using Claude Code CLI in headless mode.
